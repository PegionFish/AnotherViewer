# 执行日志（2026-09-06）——未跟踪工作文档，永不 commit

编排者按 docs/dev-plan-2026-09-06-execution-handoff.md 执行。
每卡一行：ID / 结论 / commit hash。

## W0 自举

- T0 依据文档通读：plan §1–§8 + audit 全文 + CONTEXT.md ✅
- T0 git 基线：HEAD=`a891f520`，工作树干净（仅 3 份未跟踪工作文档）✅
- T0 测试基线：前端 npm test 与后端 :anotherviewer-web:test 结果 → 见下

| 测试 | 结果 |
|------|------|
| npm test（前端） | ✅ 89 文件 / 1090 用例全绿 |
| :anotherviewer-web:test（后端） | ✅ BUILD SUCCESSFUL（44s） |

## 卡片记录

| 卡 | 结论 | commit |
|----|------|--------|
| A7-0 调查设计 | ✅ 编排者评审通过。三定案：①clearHistory 必须逐行墓碑化（App pull 增量水位，物理删行永不可达，快照差分方向相反不可用）；②download 侧列表全线缺墓碑过滤（D1-D12）+ H1-H3/Q1/F1-F3，addFavorite 需复活语义；③stamping=service 层 CurrentUsernameProvider bean（fallback "default"），worker 线程只 bump lastModified 不写 username；A7-3 用 findAllByGid+属主过滤保单行语义。产出 docs/plan-a7-design.md（工作文档不 commit） | （只读，无 commit） |
| W1-B1 首页翻转 | ✅ 后端全量 928 测试绿；报备偏离：空关键词+站点挂+无历史 → success=true/data=[]（首页不硬失败语义） | 122bbb6d |
| W1-B2 运行日志 | ✅ bootRun 实测 access log 出现、%D 有值、文档命令跑通；偏离①journald Storage=persistent 是 journald.conf 选项非 unit 属性，文档改 journald.conf.d drop-in；②AccessLogValve 相对 directory 按 catalina.base 解析，已注记 | 818ce45b |
| W1-F1 进度修复 | ✅ ReaderView 53/53 绿、typecheck 绿、全量 1095 绿；parseDeepLinkPage 统一空串语义，spec mock 改 `page=''` + reactive params | 7a2bad84 |
| W1-F2 增强图守卫 | ✅ 8/8 用例绿、typecheck 绿、全量 1097 绿；偏离：过期回调整体 no-op（不 decrement，防扣减新画廊计数） | cdcab82c |
| W1-F3 搜索键守卫 | ✅ 26 用例全绿/typecheck 绿；偏离：onMounted add 保留 + onActivated remove-before-add（KeepAlive 外 onActivated 不触发） | cab5dfb3 |
| W2-B1 下载分页后端 | ✅ 928 用例全绿；发现 all 模式后端契约未被 098f1c04 破坏（退化仅在前端），实际改动=limit 回滚 500 + 测试还原；精确契约已转告 W2-F1（offset/limit 形态） | f9ffe899 |
| A5 设置域 | ✅ 三步全落：/admin 并入 /settings/server/*（11 条 redirect）、SettingsLayout 分组侧栏重写、AdminLayout 删除、NavigationDrawer 去「管理面板」、SettingsIndex 窄屏索引页+宽屏 matchMedia replace、子页返回条（C5 模式）、横排标签条退役、live-responsive.mjs 补 4 项。全量 1123 绿；偏离：NavigationDrawer.spec.ts 入 commit（直接断言锁内组件导出，必改） | aa7cccef |
| W2-F1 下载分页前端 | ✅ 重派后判定首派半成品已实质完成，零改动续作、完整验收：DownloadView 33 用例绿、typecheck 绿、全量 1123 绿。分页条 63d20db5 形态+档位 50/100/200+all 模式混合语义+服务端 q/regex；api/download.ts 零改动（契约本就在）；虚拟滚动/排序/响应式修复保留。首派曾撞速率限制（并行 PWA 批次共享配额） | 3f2b7819 |
| W2-B2 后端性能批 | ✅ 三项全落：history q 下沉 DB+regex 字面种子预过滤、favorites slot/q 下沉 DB 分页（信封补 page/pageSize/total 向后兼容）、searchCache maximumSize(128)；结构性断言（verify 不再全表 findAll）已加；后端 934+ 全绿。偏离：FavoriteDto.kt 追认（信封新字段唯一落点，纯增量带默认值） | 91811c1c |
| W3-C1 行组件抽取 | ✅ AppListRow（336 行）+ usePagedList（199 行，双契约/stale 守卫/键盘翻页）落地，DownloadView 重构为消费方，33 护栏用例原样全绿，全量 1152 绿、typecheck 绿；偏离：下载特有样式迁入 DownloadView（DownloadItem 成孤儿组件，F5 处置）、分页条模板未抽组件（F 卡复刻） | 3a7293d6 |
| W3-F1 首页单列 | ✅ 32/32 绿、全量 1184 绿、typecheck 绿；虚拟窗口+无限滚动整族删除；偏离：page-1 适配（后端 page 是 0 起 EH 页索引非条目 offset，已核实）、单档 25、goEhSession 落 /settings/server/eh（A5 路由表现实）；编排者验收修复：onPageKey 补 viewActive 停用豁免（P1-5 同类） | 6d265642 |
| W3-F2 搜索单列 | ✅ 32/32 绿、全量 1163 绿、typecheck 绿；view-mode 键退役、stale 语义由 usePagedList 接管；偏离：条数切换单档 25（上游固定）、keyboardPaging 未启用（发现 usePagedList 监听不随 KeepAlive 摘除）→ 触发编排者修复 | 88feddb0 |
| （编排者修复） | usePagedList keyboardPaging 补 onActivated/onDeactivated 守卫（W1-F3 同款 remove-before-add）+ KeepAlive 回归用例；修 F3/DownloadView 的同类隐患 | 0a8b31eb |
| W3-F3 历史单列 | ✅ 27/27 绿、全量 1162 绿；偏离：不做虚拟滚动（pageSize 上限 200）、pageSize 不持久化（无既有偏好键）、行内无评分星（对齐 Android 轻行） | ebb5ec8e |
| W3-F4 收藏单列 | ✅ 25/25 绿、全量 1184 绿；发现 /favorite/list 控制器未暴露 pageSize（W2-B2 只改到 service）→ 保守按 20 单档落地，F4b 小卡贯通；openapi 补信封字段 | 2f87f94e |
| W3-F5 模式清理 | ✅ 删 GalleryList/Grid/Card+DownloadItem 及 spec（-3820 行），listMode 停读停写（App 同步安全性已验证：后端 section 级合并+Web 透传原对象）；1061 用例绿、零活引用；报备：types/components.ts（FROZEN 头）删已死组件接口属必要收尾；parseFavoriteSlotNames 迁 utils 保覆盖 | 88e799f1 |
| W3-X1 契约卫生 | ✅ 后端全量重跑绿（首跑一次计时型 flaky，两次重跑含 --rerun 均绿）；删 3 死端点（gallery/history、gallery/favorites、download/import-zip，grep 确认零调用）连带 Service/测试；openapi 补 restart-all/privacy-mask/site-proxy（89 paths 校验通过）；偏离：import-zip 本就不在 openapi，spec 无需删 | f13835a6 |
| A5-1 路由布局合并 | | |
| A5-2 页面平移导航 | | |
| A5-3 窄屏层级导航 | | |
| W3-F6 前端 P2 批 | ✅ 六项：①preferences dirty 失败清位+可提示 ②loadingMore 已随 A4 消失（核实无改动）③四视图 Escape/防抖 KeepAlive 停用清理（各+1 用例）④SW opaque 跳过缓存写（取舍：未缓存过的 opaque 图离线不可读）⑤401→SPA 跳转+AbortController 在途取消（动态 import router 防spec mock 崩）⑥auth/status 60s 缓存+Login 入口失效（auth store 锁外，等价接线）。前端 1077 绿 | a13209f0 |
| A7-1+2 stamping/软删 | ✅ 后端 955 全绿（含新增 WebLocalTombstoneSyncTest 集成：clearHistory 墓碑经真实 SyncService 增量 pull 可达）；偏离：addDownload/addHistory 更新/createQuickSearch/createLabel 撞墓碑均走复活（防自伤回归）；updateFavoriteSlot 无变化不写库；adopt 重触发留 INT | d8890726 |
| A7-3 属主查询 | ✅ 961 后端全量绿；merge 路径 findAllByGid+属主过滤（语义与现状逐字等价，不再炸 IncorrectResultSizeDataAccessException），写路径属主保护（他人行跳过+日志），读路径本地可见性保持（测试固化）；已知残留：GalleryLookupService/DownloadUploadService/EhImportService 三处锁外 findByGid 调用方未迁移（导入去重语义，单用户部署无触发面），记档后续处理 | e5a0ec4c |
| W3-F4b pageSize 贯通 | ✅ 后端全量绿、前端 1185 绿；控制器默认 50 钳 1..200（service 无上界钳制故在 controller 完成），前端三档落地 | 3cadc831 |
| A7-T 同步回归 | ✅ +6 用例（多用户 pull 维度/墓碑 page 不清零/复活达增量 pull/精确 tie 既有胜 + WebFavoriteTombstoneSyncTest：addFavorite stamping 可见、removeFavorite 墓碑下发）；967 全量绿；既有覆盖未重复（报告列明互引） | 460fd8b1 |
| INT-1 全量门 | ✅ typecheck 绿 + 前端 1077 全绿 + 后端 BUILD SUCCESSFUL（967，自 A7-T 后无后端改动 up-to-date 有效） | （门，无 commit） |
| INT-2 视觉基线 | ✅ 刷新 187 张（A4/A2/A5 形态），229 屏 ≤1% 阈值全稳；live-responsive 本地跑因 dev 无后端被守卫重定向 login（预期），真实数据矩阵改在 INT-4 对线上跑 | add53b3e |
| INT-3 构建部署 | ✅ build.sh（bootJar 1.1.0）→ scp app.jar.new → 备份 app.jar.bak-20260907 → 原子替换 → restart → health UP（31s 冷启动，9307 下载目录索引）；adopt 重触发完成（删 sync.ownership.adopted.default，下次同步重收养）；journald 已持久化（/var/log/journal 既有，无需配置）；data/logs/access.2026-09-07.log 出现 | （部署动作） |
| INT-4 实例验证 | ✅ 自动化部分全过：live-responsive 线上 36 张（首页站点最新单列+打码 #gid、下载页 1/177 页分页条+50 档+跳页、设置窄屏分组索引页、/admin/eh 深链 200）；慢请求命令跑通（514 请求 p50 4.5ms，发现并修正 %D 实测为微秒——deployment.md 已修 71629fc9）；打码开关往返正常。待用户设备确认：阅读进度跨设备、Web 加/删收藏→App 同步 | 71629fc9（文档） |
| （用户报障热修） | 首页首行被浮动搜索栏遮挡——.home__pagination 的 64px 清理位被同文件后序 .pagination-bar 基类 padding 简写同权重覆盖（计算值 6px，浏览器实测 nav top=0/首行 top=35 vs 搜索栏 bottom=56）。修复=双类选择器 .pagination-bar.home__pagination 恢复清理位；HomeView 28 绿+基线刷新 229 屏稳+重建部署，线上复测 overlap=0px | 1b4a89b0 |
| （用户定案热修） | 点击分区收敛：首页/搜索/历史/收藏整行→gallery 详情，「主体直达阅读」收回下载页专属（plan §4.4 遗留决策落定）；四视图 openReader 移除、spec 断言改 /gallery；全量 1077 绿+部署，线上实测首页行主体→/gallery/4173363、下载页行主体→/reader/3618832 | 5c77c8b4 |
| （用户定案热修） | 跳页输入收敛下载页专属——四视图分页条移除跳页（站点/流式列表无目标语义），页码窗口/前后页/条数切换保留；全量 1079 绿+基线刷新+部署，线上实测历史页无跳页（154 条 4 页）、下载页跳页在（177 页 8801 条） | f0058d74 |
