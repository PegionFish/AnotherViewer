# 执行日志（2026-09-08）——图像处理×EntryPoint 集成批

编排者按 docs/dev-plan-2026-09-08-image-processing-execution-handoff.md 执行（多子代理并行协议：W1/W2 各三代理同波并行，主模型统一 commit）。
前置冒烟（规划期已做）：141:9800 EntryPoint 实测——rembg/remove_bg 端到端 5s completed、产物 302→RGBA PNG；部署版无 `/api/v1/tasks`（用 result 端点）、无 image→image 超分（realesr 对图无响应）→ REMOVE_BG 定为已验证默认能力（手册 §1/R1）。

## 测试基线与波门

| 门 | 结果 |
|------|------|
| W0 门 | 后端 :anotherviewer-web:test 全绿；前端 1108 用例 + typecheck 绿 |
| W1 门（C1+C2+C3 后） | 后端全量绿；前端 1115 用例 + typecheck 绿 |
| W2 门（C4+C5+C6 后） | 后端全量绿；前端 1124 用例 + typecheck 绿 |
| W3 视觉 | test:visual:update 重录 → compare **229 屏全过**（阈值 1%） |

## 卡片记录

| 卡 | 结论 | commit |
|----|------|--------|
| C0 契约冻结 | ✅ REMOVE_BG 枚举、EntryPoint 五操作接口+归一化词表（EpTaskState/EpException）、processing_task 实体/仓库/store 接口（donePages CSV 列为页级去载数据源）、settings 五新键接线（token 走 enc:v1: 加密，GET 只回 entrypointTokenSet）、openapi ProcessingSettings、前端类型+api/processing.ts、mockwebserver 4.12.0、yml scan-interval-ms/task-timeout-minutes。插曲：首版仓库查询引用不存在的 donePages 列致 SqliteIndexDdlTest 上下文失败，补列即修；ImageProxyControllerTest 12v11 为负载抖动（单跑复绿） | a776eb4b |
| C1 适配域（代理 A） | ✅ OkHttpEntryPointClient（Bearer 空 token 不带头/probeClient 模板代理链/429+5xx 指数退避 1s×2 至多 3 重试/信封三阶梯解析/302 产物跟随/cancel 永不抛）+ CapabilityMapper（D5 默认映射表+type_mapping JSON 叠加+60s TTL 缓存+NOT_FOUND 强刷）+ EntryPointProcessor（upload→submit→轮询 1s×1.5→5s 封顶→超时先 cancel→产物临时文件+ImageIO 按需重编码 RGBA→JPEG 铺白底）。31 用例绿。偏离追认：callTimeout poll 10s/其余 180s/cancel 10s（覆盖 D16 字面值，业务超时 30min 仍是主闸）；downloadToFile 为接口外公开方法+ArtifactDownloader seam；新增错误码 EP_UNMAPPED/EP_FAILED | 6282b95c |
| C2 持久化域（代理 B） | ✅ JpaProcessingTaskStore（record↔entity 双向、upsert 不增行、findHistory size 钳 1..500、findCompletedPages 多行 CSV 合并容错、markInterruptedAtStartup）+ DDL 索引测试 + @DataJpaTest 7 用例。预授权偏离：ProcessingTaskRecord 末尾追加 donePages: Set<Int>（纯增量）；error_message 截断落 store 映射层 | 6282b95c |
| C3 前端 API 域（代理 C） | ✅ processing.ts 复核零改动（C0 一步到位）；7 用例（URL/params/空容错/state 条件键/retry 409 错误码提取） | 6282b95c |
| C4 编排自动化域（代理 D） | ✅ ImageProcessingService 改造（submitGallery +trigger/force、页级去重 D8、写穿 D10、错误归档 D12、resolveInputImage 先 DownloadDirIndex 后 cache、临时产物清理）+ getActiveRecords/retryTask + ProcessingAutomation（DownloadProgress state=3 监听、@Scheduled 心跳+lastScan 间隔闸、每轮 ≤3 gallery 防雪崩、@PostConstruct 对账）。47 用例绿（含更新既有 27）。偏离追认：store/downloadDirIndex 可空默认参注入（旧测试零改动）；retry 只复原 processingType（record 无 format/quality 列，遗留小项）；title 暂空串（GalleryLookupService 无标题 API，前端回落「画廊 #id」）；定期闸只查 periodic_enabled | 67973c33 |
| C5 端点契约域（代理 E） | ✅ GET /process/tasks?active=1（内存活跃）/无参走 store 近 200 条、GET /process/history（size 钳 1..500、state 非法 400 PARAM_INVALID）、POST /process/retry/{taskId}（404/409 信封）；ProcessingTaskRecordDto 18 字段+from() 映射（donePages 不外泄）；openapi 三 path+三 schema+ProcessingRequest 补 REMOVE_BG。15 用例绿。追认：ProcessingControllerTest 自 api/ 迁 processing/ 包并删旧副本（编排者给的模板路径笔误所致，编译必需）；C4 落地与钉死契约逐条互验吻合 | 67973c33 |
| C6 页面三区块（代理 F） | ✅ AdminProcessing.vue：自动化段（EntryPoint 地址/token password 草稿永不回填/下载自动处理/定期补跑+周期下拉）+进行中任务（3s 轮询、进度/当前页/耗时/源→目标等宽/红错误行/取消）+历史记录（状态过滤/20 条分页边界禁用/重试+双列表刷新/错误明细）。12 用例绿、typecheck 0 错、全量 1124 绿。偏离追认：settings.ts 补 entrypointToken?: string 写入专用类型；当前页以 pagesDone+1 呈现（record 无 currentPage） | 67973c33 |
| C8 视觉+文档 | ✅ admin-processing 12 张基线重录 + reader 12 张亚阈值渲染噪声固化（34 字节级）；compare 229/229 过；scripts/entrypoint-smoke.sh（token 仅经 EP_TOKEN 环境变量，D20） | （本 commit） |
| C9/C10 部署实弹 | 待 W4 | 待续 |

## 遗留小项（不阻塞上线）

1. retry 不复原 output_format/quality（record 缺列）——后续给 processing_task 增列可闭合。
2. 任务 title 恒空串——GalleryLookupService 无标题 API；监控页显示「画廊 #id」，后续可从下载行取。
3. EntryPoint 侧图像超分缺位（R1）——UPSCALE_2X/4X 映射指向 realesr/upscale（video 模块）实弹大概率失败，错误会如实出现在监控页；真解在 EntryPoint 侧加 image 模块。
4. epTaskIds 列暂空（processor 未回传 EntryPoint task_id，冻结契约未含通道）——调试靠 errorCode/errorMessage。
